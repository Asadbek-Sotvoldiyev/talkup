# Django sozlamalari
DJANGO_SECRET_KEY=yangi-maxfiy-kalit-bu-yerga
DEBUG=False

# Ruxsat berilgan hostlar (vergul bilan ajrating)
ALLOWED_HOSTS=talkup.uz,www.talkup.uz

# CSRF ishonchli manzillar (vergul bilan ajrating)
CSRF_TRUSTED_ORIGINS=https://talkup.uz,https://www.talkup.uz

# Xabar shifrlash kaliti (Fernet base64 kalit)
# Yangi kalit: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MESSAGE_ENCRYPTION_KEY=yangi-fernet-kalit-bu-yerga

# Redis URL (channel layer va cache uchun)
REDIS_URL=redis://127.0.0.1:6379
